<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<% if (title) { %>
    <title><%= title %></title>
<% } else { %>
    <title>Mercado Liebre</title>
<% } %>

<% stylesheets.forEach(function (ss) { %>
    <link rel="stylesheet" href="<%= ss.url %>">
<% }) %>
